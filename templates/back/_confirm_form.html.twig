<form method="post" action="{{ path('user_confirm', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to confirm this user ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.student.id) }}">
    <button class="btn btn-success">confirm</button>
</form>