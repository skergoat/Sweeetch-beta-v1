{{ form_start(formPassword) }}

<div class="col-lg-6 col-md-12 col-sm-12 col-xs-12" style="margin:auto;">
    <div class="form-group">
        {{ form_row(formPassword.user.password, {'attr' : { class: 'form-control' }} ) }}
        {# {{ form_row(form.user.password.first, {'attr' : { class: 'form-control' }} ) }}
        {{ form_row(form.user.password.second, {'attr' : { class: 'form-control' }} ) }} #}
    </div>
    <div class="form-group"></div>
        <button class="btn btn-primary">Mettre à jour</button>
    </div>
</div>

{{ form_end(formPassword) }}
