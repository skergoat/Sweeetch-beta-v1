{{ form_start(formPassword) }}

<div class="col-lg-6 col-md-12 col-sm-12 col-xs-12" style="margin:auto;">
    <div class="form-group">        
         {{ form_row(formPassword.user.password.first, {'type': 'password', 'label' : 'Nouveau mot de passe', 'attr' : { class: 'form-control', value:'' }} ) }}
    </div>
    <div class="form-group">
        {{ form_row(formPassword.user.password.second, {'type': 'password', 'label' : 'Répéter le mot de passe', 'attr' : { class: 'form-control', value:'' }} ) }} 
    </div>
    <div class="form-group"></div>
        <button class="btn btn-primary">Mettre à jour</button>
    </div>
</div>

{{ form_end(formPassword) }}
