
{% extends 'base-student.html.twig' %}

{% block title %} {{ parent() }} - offers {% endblock %}

{% block body %}

<br><br>

    {% if is_granted('ROLE_HIRED') %}
        List Schools
    {% else %}
    <div class="col-lg-6">
        <div class="info-box mb-3 bg-warning">
            <span class="info-box-icon"><i class="fas fa-info"></i></span>
            <div class="info-box-content" id="box-flex">
                <span class="info-box-text">Vous devez être embauché pour avoir accès aux écoles</span>
            </div>
        </div>
    </div>
    {% endif %}

{% endblock %}