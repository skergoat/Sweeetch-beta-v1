{{ form_start(form) }}
<div class="form-group">
    {{ form_row(form.email, {'label':'Mail', 'attr' : { class: 'form-control' }} ) }}
</div>
<div class="form-group">
    {{ form_row(form.password, {'label':'Mot de passe', 'attr' : { class: 'form-control' }} ) }}
</div>
    <button class="btn btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
